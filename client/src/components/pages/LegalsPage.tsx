import React from 'react';
import { Container } from 'react-bootstrap';
import LegalsList from '../ui/LegalsList';

export default function LegalsPage(): JSX.Element {
  return (
    <Container >
      <LegalsList />
    </Container>
  );
}
